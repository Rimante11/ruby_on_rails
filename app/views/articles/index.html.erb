<p style="color: green"><%= notice %></p>

<h1>Articles listing page</h1>

<%#
<div id="articles">
  <% @articles.each do |article| %>
<%#  <%= render article %>
<%#  <p>
      <%= link_to "Show this article", article %>
<%#   </p>
  <% end %>
<%#</div>

<%= link_to "New article", new_article_path %>
<br>
<br>



<%#------------ Extra code -----------%>
<%= link_to 'Home page', root_path %><br>
<br>
<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th colspan="3">Actions</th>
    </tr>
  </thead>

  <tbody>
    <% @articles.each do |article| %>
      <tr>
        <td><%= article.title %></td>
        <td><%= article.description %></td>
        <td><%= link_to 'Show', article_path(article) %></td>
        <td><%= link_to 'Edit', edit_article_path(article), method: :edit %></td>
        <%#in article_path(article.id) med går bra med bara article, rails smart för det%>
        <%#link_to 'Delete' does not work darför skapat button_to %>
        <td><%= button_to 'Delete', article_path(article), method: :delete, 
          form: { data: { turbo_confirm: "Are you sure?" } } 
        %> <%#article_path(article), method: :delete %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<p>
  <%= link_to 'Create new article', new_article_path %>
</p>
